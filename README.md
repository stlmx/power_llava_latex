# PowerBrain TII Submission Source

Main manuscript source:

- `TII-Articles-LaTeX-template/tii-articles-template.tex`
- `TII-Articles-LaTeX-template/refs.bib`

Build locally with:

```bash
./build.sh
```

Required figure assets are stored at the repository root, and representative benchmark images are stored in `table_examples/`.
